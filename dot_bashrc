[[ $- != *i* ]] && return
[[ $(ps -o comm= -p $PPID) == fish ]] && return
[[ $SHLVL -ne 1 ]] && return
[[ $TERM == linux ]] && return

shopt -q login_shell && LOGIN_OPTION='--login' || LOGIN_OPTION=''
exec fish $LOGIN_OPTION
